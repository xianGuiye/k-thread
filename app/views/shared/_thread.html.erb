<% if @thread_items.any? %>
<ol class="topics">
  <%= render @thread_items %>
</ol>
<%= will_paginate @thread_items, previous_label: '&#8592; &nbsp;前のページ', next_label: '次のページ &#8594;', inner_window: 1, outer_window: 0 %>
<% end %>